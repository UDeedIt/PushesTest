package pro.udeedit.devtools.pushestest.utils

// Pushes Preferences
enum class AppSetting {
    // Boolean Settings
    MOCK_DATA,
    OVERWRITE,
    PERSISTENT,
    MULTILINE,
    FULL_SCREEN,
    GROUPED,
    BIG_TEXT,
    BIG_PICTURE,
    INBOX_STYLE,
    ACTIONS,
    SUBTEXT,
    LARGE_ICON,
    CHRONOMETER,
    VIBRATION,
    SOUND,

    // Dropdown Settings
    PERIODS,
    DELAYS,
    IMPORTANCE,
    VISIBILITY
}
